langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.tipologia_eventi
id: taxonomy_term.tipologia_eventi
target_entity_type_id: taxonomy_term
target_bundle: tipologia_eventi
default_langcode: it
language_alterable: true

