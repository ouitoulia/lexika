langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.tipologia_progetti
id: taxonomy_term.tipologia_progetti
target_entity_type_id: taxonomy_term
target_bundle: tipologia_progetti
default_langcode: it
language_alterable: true

