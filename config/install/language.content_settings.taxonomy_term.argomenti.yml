langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.argomenti
id: taxonomy_term.argomenti
target_entity_type_id: taxonomy_term
target_bundle: argomenti
default_langcode: it
language_alterable: true

