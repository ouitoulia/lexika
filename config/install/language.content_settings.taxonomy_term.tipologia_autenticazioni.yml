langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.tipologia_autenticazioni
id: taxonomy_term.tipologia_autenticazioni
target_entity_type_id: taxonomy_term
target_bundle: tipologia_autenticazioni
default_langcode: it
language_alterable: true

