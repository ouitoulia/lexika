langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.cosa_serve
id: taxonomy_term.cosa_serve
target_entity_type_id: taxonomy_term
target_bundle: cosa_serve
default_langcode: it
language_alterable: true
