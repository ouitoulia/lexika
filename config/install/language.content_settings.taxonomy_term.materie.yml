langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.materie
id: taxonomy_term.materie
target_entity_type_id: taxonomy_term
target_bundle: materie
default_langcode: it
language_alterable: true

