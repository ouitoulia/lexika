langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.tipologia_supplenze
id: taxonomy_term.tipologia_supplenze
target_entity_type_id: taxonomy_term
target_bundle: tipologia_supplenze
default_langcode: it
language_alterable: true
