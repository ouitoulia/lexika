langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.tipologia_luoghi
id: taxonomy_term.tipologia_luoghi
target_entity_type_id: taxonomy_term
target_bundle: tipologia_luoghi
default_langcode: it
language_alterable: true

