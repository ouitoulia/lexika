langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.tipologia_licenze
id: taxonomy_term.tipologia_licenze
target_entity_type_id: taxonomy_term
target_bundle: tipologia_licenze
default_langcode: it
language_alterable: true

