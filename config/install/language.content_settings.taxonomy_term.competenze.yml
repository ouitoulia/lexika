langcode: it
status: true
dependencies:
  config:
    - taxonomy.vocabulary.competenze
id: taxonomy_term.competenze
target_entity_type_id: taxonomy_term
target_bundle: competenze
default_langcode: it
language_alterable: true

